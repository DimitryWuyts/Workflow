{% extends 'base.html.twig' %}

{% block title %}Hello CustController!{% endblock %}

{% block body %}
    <style>
        .example-wrapper { margin: 1em auto; max-width: 800px; width: 95%; font: 18px/1.5 sans-serif; }
        .example-wrapper code { background: #F5F5F5; padding: 2px 6px; }
    </style>


    {{ form_start(ticketForm) }}
    {{ form_row(ticketForm.ticketTitle) }}
   {# {{ form_row(ticketForm.status) }}#}
    {#{{ form_row(ticketForm.level) }}#}
    {{ form_row(ticketForm.date) }}
    {{ form_row(ticketForm.content) }}
{#    {{ form_row(ticketForm.counter) }}
    {{ form_row(ticketForm.prioritylevel) }}#}


    <button class="btn">Register</button>
    {{ form_end(ticketForm) }}
{% endblock %}
